server:
  port: 5050

logging:
  level:
    org:
      apache:
        kafka: warn
    io:
      github:
        alikelleci:
          easysourcing: debug

spring:
  application:
    name: "example-app"
  kafka:
    bootstrap-servers: "localhost:9092"
